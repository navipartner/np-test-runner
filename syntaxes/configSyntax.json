{
    "$id": "AL Test Runner Config File",
    "$schema": "http://json-schema.org/draft-07/schema",
    "description": "Schema for AL Test Runner config file",
    "type": "object",
    "properties": {
        "launchConfigName": {
            "type": "string",
            "description": "The name of the launch configuration to test against"
        },
        "attachConfigName": {
            "type": "string",
            "description": "The name of the attach configuration to debug test against"
        },
        "codeCoveragePath": {
            "type": "string",
            "description": "The path to save details of code coverage to e.g. .//.npaltestrunner//codecoverage.json"
        },
        "culture": {
            "type": "string",
            "description": "The culture to run tests with. Defaults to en-US."
        }
    }
}